# FERender 示例

这里的示例用于验证和说明 **安装后的 FERender 渲染引擎** 如何被外部 Qt 项目调用。

| 示例 | 说明 |
|------|------|
| `simple_viewer` | 独立 CMake/Qt 小界面，通过 `find_package(FERender)` 调用安装后的渲染引擎 |

先从仓库根目录构建并安装 FERender，再进入具体示例目录查看 README。
